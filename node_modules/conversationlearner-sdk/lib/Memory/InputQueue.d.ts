/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
import * as BB from 'botbuilder';
import { BotState } from './BotState';
export interface QueuedInput {
    conversationId: string;
    timestamp: number;
    callback: Function;
}
export declare class InputQueue {
    private static messageQueue;
    static AddInput(botState: BotState, request: BB.Activity, conversationReference: Partial<BB.ConversationReference>, callback: Function): Promise<any>;
    private static InputQueueAdd(conversationId, callback);
    private static InputQueueProcess(botState);
    private static InputQueueProcessNext(botState);
    static MessageHandled(botState: BotState, conversationId: string | undefined): Promise<void>;
}
