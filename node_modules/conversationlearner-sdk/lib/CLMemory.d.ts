/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
import * as BB from 'botbuilder';
import { BotMemory } from './Memory/BotMemory';
import { BotState } from './Memory/BotState';
import { AppBase } from 'conversationlearner-models';
export declare class CLMemory {
    private static memoryStorage;
    private memCache;
    private userkey;
    static Init(memoryStorage: BB.Storage | null): void;
    private constructor();
    static GetMemory(key: string): CLMemory;
    static InitMemory(user: BB.ChannelAccount, conversationReference: Partial<BB.ConversationReference>): Promise<CLMemory>;
    private Key(datakey);
    GetAsync(datakey: string): Promise<any>;
    SetAsync(datakey: string, value: any): Promise<void>;
    DeleteAsync(datakey: string): Promise<void>;
    SetAppAsync(app: AppBase | null): Promise<void>;
    readonly BotMemory: BotMemory;
    readonly BotState: BotState;
}
