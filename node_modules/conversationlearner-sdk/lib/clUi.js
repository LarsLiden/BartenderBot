"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
const express = require("express");
const clUI = require("conversationlearner-ui");
function default_1(port = 5050) {
    console.log(`conversationlearner-ui directory path: `, clUI.directoryPath);
    console.log(`conversationlearner-ui default file path: `, clUI.defaultFilePath);
    const app = express();
    app.use(express.static(clUI.directoryPath)).use((req, res) => res.sendFile(clUI.defaultFilePath));
    const listener = app.listen(port, () => console.log(`Navigate to http://localhost:${listener.address().port} to view Conversation Learner administration application.`));
    return {
        app,
        listener
    };
}
exports.default = default_1;
//# sourceMappingURL=clUi.js.map