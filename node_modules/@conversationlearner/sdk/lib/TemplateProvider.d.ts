import { Template, RenderedActionArgument } from '@conversationlearner/models';
export declare class TemplateProvider {
    private static hasSubmitError;
    static RenderTemplate(templateName: string, templateArguments: RenderedActionArgument[]): Promise<any | null>;
    static GetTemplates(): Template[];
    private static UniqueTemplateVariables(template);
    static RemoveEmptyArguments(formString: string): string;
    static GetArgumentNames(formString: string): string[];
    static TemplateDirectory(): string;
    static GetTemplate(templateName: string): any;
    static GetTemplatesNames(): string[];
    private static GetVarNames(template);
    private static GetTemplateVariables(template);
}
